node_modules/
dist/
coverage/
*.tsbuildinfo
*.log
*.tgz

# secrets / local env (broker creds live in the harness MCP config, never the repo)
.env
.env.*

# editor / OS
.vscode/
.idea/
.DS_Store

# project-local skill installs (not source). Every harness provider dir
# cupel can target — keep this in sync with PROVIDER_DIRS in cli/src/skills.ts.
.agents/
.claude/
.cursor/
.gemini/
.github/skills/
.kiro/
.opencode/
.pi/
.qoder/
.trae/
skills-lock.json
