node_modules/
dist/
coverage/
*.tsbuildinfo
*.log
*.tgz

# secrets / local env (broker creds live in the harness MCP config, never the repo)
.env
.env.*

# editor / OS
.vscode/
.idea/
.DS_Store

# Harness skill copies (.claude/skills, .cursor/skills, etc.) are committed so
# skills.sh and each agent can discover the skill. They mirror skills/cupel;
# re-sync with `cupel skills install --all` after editing the source.

# the skills CLI's lockfile is local state, not source
skills-lock.json
